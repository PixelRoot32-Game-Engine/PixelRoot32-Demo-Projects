// The engine is installed for dialog_test but not built (see platformio.ini),
// so the one engine translation unit the adapters link against is compiled
// here, straight from the installed checkout.
#include "gameplay/DialogRunner.cpp"
